server {
  listen 8443 ssl;
  server_name localhost;
  server_tokens off;

  ssl_certificate /run/secrets/mnema_local_tls_cert;
  ssl_certificate_key /run/secrets/mnema_local_tls_key;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_session_tickets off;

  resolver 127.0.0.11 ipv6=off valid=10s;
  set $learning_upstream learning:8080;

  root /usr/share/nginx/html;
  add_header_inherit merge;
  include /etc/nginx/conf.d/security-headers.inc;

  gzip on;
  gzip_comp_level 5;
  gzip_min_length 256;
  gzip_vary on;
  gzip_types text/plain text/css application/json application/javascript application/xml image/svg+xml;

  location ^~ /api/ {
    proxy_pass http://$learning_upstream;
    proxy_http_version 1.1;
    proxy_set_header Host $http_host;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $http_host;
    proxy_set_header Cookie "";
    proxy_hide_header Set-Cookie;
    proxy_connect_timeout 2s;
    proxy_read_timeout 10s;
    proxy_send_timeout 10s;
    proxy_buffering off;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }

  include /etc/nginx/conf.d/ai-route.inc;

  location = /index.html {
    add_header Cache-Control "public, max-age=0, must-revalidate";
    try_files $uri =404;
  }

  location = /app-config.js {
    add_header Cache-Control "no-store";
    try_files $uri =404;
  }

  location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|ico|woff2?)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
  }
}

server {
  listen 8444 ssl;
  server_name localhost frontend;
  server_tokens off;

  ssl_certificate /run/secrets/mnema_local_tls_cert;
  ssl_certificate_key /run/secrets/mnema_local_tls_key;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_session_tickets off;

  resolver 127.0.0.11 ipv6=off valid=10s;
  set $identity_upstream identity-account:8081;

  location / {
    proxy_pass http://$identity_upstream;
    proxy_http_version 1.1;
    proxy_set_header Host $http_host;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $http_host;
    proxy_connect_timeout 2s;
    proxy_read_timeout 10s;
    proxy_send_timeout 10s;
    proxy_buffering off;
  }
}
