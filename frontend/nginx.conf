server {
  listen 80;
  server_name _;
  server_tokens off;
  root /usr/share/nginx/html;

  # Preserve the generated response-security headers inside locations that add
  # their own cache headers. Supported by the pinned nginx 1.31 image.
  add_header_inherit merge;
  include /etc/nginx/conf.d/security-headers.inc;

  gzip on;
  gzip_comp_level 5;
  gzip_min_length 256;
  gzip_vary on;
  gzip_types
    text/plain
    text/css
    text/html
    application/json
    application/javascript
    application/xml
    application/xml+rss
    application/manifest+json
    image/svg+xml;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Generated at container startup from MNEMA_FEATURE_AI_ENABLED.
  include /etc/nginx/conf.d/ai-route.inc;

  location = /index.html {
    add_header Cache-Control "public, max-age=0, must-revalidate";
    try_files $uri =404;
  }

  location = /manifest.webmanifest {
    add_header Cache-Control "public, max-age=3600, must-revalidate";
    try_files $uri =404;
  }

  location = /assets/og-image.png {
    add_header Cache-Control "public, max-age=0, must-revalidate";
    try_files $uri =404;
  }

  location = /app-config.js {
    add_header Cache-Control "no-store";
    try_files $uri =404;
  }

  location = /favicon.ico {
    add_header Cache-Control "public, max-age=0, must-revalidate";
    try_files $uri =404;
  }

  location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|ico|woff2?)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
  }
}
