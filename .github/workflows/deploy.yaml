name: Main CI

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions: {}

jobs:
  validate-main-ref:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Require exact main branch
        run: |
          if [ "$GITHUB_REF" != refs/heads/main ]; then
            echo '::error::Main CI may run only for refs/heads/main.'
            exit 1
          fi

  backend-quality:
    needs: validate-main-ref
    runs-on: ubuntu-latest
    concurrency:
      group: main-ci-backend-${{ github.ref }}
      cancel-in-progress: true
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK 21
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # v6.0.0
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Run backend quality gate
        working-directory: backend
        run: ./gradlew quality

      - name: Upload backend coverage report
        uses: ./.github/actions/safe-upload-artifact
        with:
          policy-id: main-backend-jacoco
          name: backend-jacoco-report
          path: backend/build/reports/jacoco/jacocoRootReport/
          retention-days: 30
          if-no-files-found: warn
          classification: internal-test-report

  frontend-quality:
    needs: validate-main-ref
    runs-on: ubuntu-latest
    concurrency:
      group: main-ci-frontend-${{ github.ref }}
      cancel-in-progress: true
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Verify GitHub automation policies
        run: |
          python3 scripts/verify_github_actions_pins.py
          python3 scripts/verify_security_automation_policy.py
          python3 scripts/verify_artifact_security_policy.py
          python3 scripts/verify_production_image_pins.py
          python3 -m unittest discover -s scripts/tests -p 'test_verify_*.py' -v

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22.23.2"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Run frontend lint
        working-directory: frontend
        run: npm run lint

      - name: Run frontend tests
        working-directory: frontend
        run: npm run test

      - name: Run frontend build
        working-directory: frontend
        run: npm run build

      - name: Verify frontend release contract
        run: ./scripts/test-frontend-release-contract.sh

      - name: Verify browser response-security contract
        run: ./scripts/test-browser-security-headers.sh

      - name: Verify release manifest renderer
        run: ./scripts/test-render-release-manifest.sh

      - name: Verify canonical production release diff
        run: ./scripts/test-capture-release-diff.sh

      - name: Verify value-silent Kubernetes Secret drift detection
        run: ./scripts/test-detect-kubernetes-secret-drift.sh

      - name: Verify approved Secret snapshot binding
        run: ./scripts/test-secret-snapshot-binding.sh

      - name: Verify bootstrap-only Secret rotation guard
        run: ./scripts/test-kubernetes-bootstrap-secret-values.sh

      - name: Verify environment credential separation contract
        run: ./scripts/test-environment-secret-separation.sh

      - name: Verify production deployment safety contract
        run: ./scripts/test-production-deploy-safety.sh

      - name: Verify exact live release binding and conditional Secret replace
        run: ./scripts/test-kubernetes-live-release-binding.sh

      - name: Verify staging plan is fully previewed before mutation
        run: ./scripts/test-staging-plan-preview.sh

      - name: Verify idempotent Kubernetes Secret consumer reconciliation
        run: ./scripts/test-kubernetes-secret-consumer-reconciliation.sh

      - name: Verify fail-closed Kubernetes Secret rollback
        run: ./scripts/test-kubernetes-secret-rollback.sh

      - name: Verify production telemetry host-boundary contract
        run: ./scripts/test-production-telemetry-boundary.sh

      - name: Verify release security evidence contract
        run: ./scripts/test-release-security-contract.sh

      - name: Verify environment isolation contract
        run: ./scripts/test-deployment-contract.sh

      - name: Verify staging kubeconfig contract
        run: ./scripts/test-create-staging-kubeconfig.sh

      - name: Verify staging host firewall contract
        run: ./scripts/test-staging-host-firewall.sh

      - name: Verify staging TLS boundary
        run: ./scripts/test-staging-tls-boundary.sh

      - name: Verify fail-closed staging bootstrap helper
        run: ./scripts/test-apply-staging-bootstrap.sh

      - name: Run release automation unit tests
        run: python3 -m unittest discover -s scripts/smoke/tests -v

      - name: Verify database backup and recovery contract
        run: ./scripts/test-backup-contract.sh

      - name: Verify scoped recovery kubeconfig contract
        run: ./scripts/test-create-recovery-kubeconfig.sh

      - name: Run database recovery unit tests
        run: PYTHONPATH=scripts/backup python3 -m unittest discover -s scripts/backup/tests -v

      - name: Run PostgreSQL 16 to 18 recovery integration test
        run: ./scripts/test-backup-integration.sh

  build-and-push:
    needs:
      - backend-quality
      - frontend-quality
    runs-on: ubuntu-latest
    concurrency:
      group: main-ci-build-${{ github.ref }}-${{ matrix.name }}
      cancel-in-progress: true
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    strategy:
      matrix:
        include:
          - name: identity-account
            context: ./backend
            dockerfile: ./backend/Dockerfile
            target: identity-account-runtime

          - name: learning
            context: ./backend
            dockerfile: ./backend/Dockerfile
            target: learning-runtime

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Repo slug (lowercase)
        id: repo
        run: echo "REPO=${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      - name: Compute image tag
        id: meta
        run: echo "TAG=sha-${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Build & push backend ${{ matrix.name }}
        id: backend-image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          target: ${{ matrix.target }}
          push: true
          tags: |
            ghcr.io/${{ steps.repo.outputs.REPO }}/${{ matrix.name }}:${{ steps.meta.outputs.TAG }}
            ghcr.io/${{ steps.repo.outputs.REPO }}/${{ matrix.name }}:latest
          cache-from: type=gha,scope=backend
          cache-to: type=gha,mode=max,scope=backend
          provenance: mode=max
          sbom: true

      - name: Record immutable image digest
        id: release-image
        env:
          BACKEND_DIGEST: ${{ steps.backend-image.outputs.digest }}
          IMAGE_NAME: ghcr.io/${{ steps.repo.outputs.REPO }}/${{ matrix.name }}
          SERVICE: ${{ matrix.name }}
        run: |
          digest="$BACKEND_DIGEST"
          case "$digest" in
            sha256:*) digest_hex=${digest#sha256:} ;;
            *)
              echo "::error::Missing or invalid digest for ${SERVICE}: ${digest}"
              exit 1
              ;;
          esac
          case "$digest_hex" in
            *[!0-9a-f]*)
              echo "::error::Invalid digest for ${SERVICE}: ${digest}"
              exit 1
              ;;
          esac
          if [ "${#digest_hex}" -ne 64 ]; then
            echo "::error::Unexpected digest length for ${SERVICE}"
            exit 1
          fi
          mkdir -p "${RUNNER_TEMP}/release-digests"
          printf '%s\n' "$digest" > "${RUNNER_TEMP}/release-digests/${SERVICE}.digest"
          {
            echo "digest=${digest}"
            echo "image-name=${IMAGE_NAME}"
            echo "image=${IMAGE_NAME}@${digest}"
          } >> "$GITHUB_OUTPUT"

      - name: Extract BuildKit SPDX SBOM for the exact digest
        env:
          IMAGE: ${{ steps.release-image.outputs.image }}
          SERVICE: ${{ matrix.name }}
        run: |
          mkdir -p "$RUNNER_TEMP/release-security"
          docker buildx imagetools inspect "$IMAGE" \
            --format '{{ json .SBOM.SPDX }}' \
            > "$RUNNER_TEMP/release-security/${SERVICE}-sbom.spdx.json"

      - name: Create GitHub build provenance attestation
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-name: ${{ steps.release-image.outputs.image-name }}
          subject-digest: ${{ steps.release-image.outputs.digest }}
          push-to-registry: true

      - name: Create GitHub SBOM attestation
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-name: ${{ steps.release-image.outputs.image-name }}
          subject-digest: ${{ steps.release-image.outputs.digest }}
          sbom-path: ${{ runner.temp }}/release-security/${{ matrix.name }}-sbom.spdx.json
          push-to-registry: true

      - name: Verify GitHub attestations against release identity
        env:
          GH_TOKEN: ${{ github.token }}
          IMAGE: ${{ steps.release-image.outputs.image }}
          SERVICE: ${{ matrix.name }}
        run: |
          gh attestation verify "oci://${IMAGE}" \
            --repo "$GITHUB_REPOSITORY" \
            --signer-workflow "$GITHUB_REPOSITORY/.github/workflows/deploy.yaml" \
            --source-digest "$GITHUB_SHA" \
            --source-ref refs/heads/main \
            --deny-self-hosted-runners \
            --format json \
            > "$RUNNER_TEMP/release-security/${SERVICE}-provenance-verification.json"
          gh attestation verify "oci://${IMAGE}" \
            --repo "$GITHUB_REPOSITORY" \
            --signer-workflow "$GITHUB_REPOSITORY/.github/workflows/deploy.yaml" \
            --source-digest "$GITHUB_SHA" \
            --source-ref refs/heads/main \
            --deny-self-hosted-runners \
            --predicate-type https://spdx.dev/Document/v2.3 \
            --format json \
            > "$RUNNER_TEMP/release-security/${SERVICE}-sbom-verification.json"

      - name: Scan exact release digest with Trivy
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          scan-type: image
          image-ref: ${{ steps.release-image.outputs.image }}
          scanners: vuln
          format: json
          output: ${{ runner.temp }}/release-security/${{ matrix.name }}-trivy-report.json
          severity: UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL
          ignore-unfixed: "false"
          exit-code: "0"
          timeout: 10m
          version: v0.70.0
          cache: true
          trivyignores: security/trivy-release-ignore

      - name: Record scanner and vulnerability database identity
        env:
          IMAGE: ${{ steps.release-image.outputs.image }}
          SERVICE: ${{ matrix.name }}
        run: |
          trivy image "$IMAGE" \
            --cache-dir "$GITHUB_WORKSPACE/.cache/trivy" \
            --skip-db-update \
            --ignorefile security/trivy-release-ignore \
            --scanners vuln \
            --format sarif \
            --output "$RUNNER_TEMP/release-security/${SERVICE}-trivy-results.sarif" \
            --severity UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL \
            --exit-code 0
          trivy version \
            --cache-dir "$GITHUB_WORKSPACE/.cache/trivy" \
            --format json \
            > "$RUNNER_TEMP/release-security/${SERVICE}-trivy-version.json"

      - name: Enforce release vulnerability policy
        env:
          IMAGE: ${{ steps.release-image.outputs.image }}
          SERVICE: ${{ matrix.name }}
        run: |
          python3 scripts/verify_release_security_evidence.py evaluate \
            --service "$SERVICE" \
            --image "$IMAGE" \
            --repository "$GITHUB_REPOSITORY" \
            --source-commit "$GITHUB_SHA" \
            --build-run-id "$GITHUB_RUN_ID" \
            --build-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --report "$RUNNER_TEMP/release-security/${SERVICE}-trivy-report.json" \
            --sarif "$RUNNER_TEMP/release-security/${SERVICE}-trivy-results.sarif" \
            --scanner-metadata "$RUNNER_TEMP/release-security/${SERVICE}-trivy-version.json" \
            --exceptions security/release-image-exceptions.json \
            --trivy-ignore security/trivy-release-ignore \
            --sbom "$RUNNER_TEMP/release-security/${SERVICE}-sbom.spdx.json" \
            --provenance-verification "$RUNNER_TEMP/release-security/${SERVICE}-provenance-verification.json" \
            --sbom-verification "$RUNNER_TEMP/release-security/${SERVICE}-sbom-verification.json" \
            --output "$RUNNER_TEMP/release-security/${SERVICE}-security-evidence.json"

      - name: Upload full per-image security evidence
        if: ${{ always() }}
        uses: ./.github/actions/safe-upload-artifact
        with:
          policy-id: main-release-security
          name: release-security-${{ matrix.name }}
          path: ${{ runner.temp }}/release-security/${{ matrix.name }}-*
          if-no-files-found: warn
          retention-days: 30
          classification: security-evidence

      - name: Upload immutable image digest
        uses: ./.github/actions/safe-upload-artifact
        with:
          policy-id: main-release-image-digest
          name: release-image-${{ matrix.name }}
          path: ${{ runner.temp }}/release-digests/${{ matrix.name }}.digest
          if-no-files-found: error
          retention-days: 30
          classification: release-identifier

  render-release:
    needs: build-and-push
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    steps:
      - name: Checkout release revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Download image digests
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: release-image-*
          path: ${{ runner.temp }}/release-digests
          merge-multiple: true

      - name: Download verified per-image security evidence
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: release-security-*
          path: ${{ runner.temp }}/release-security
          merge-multiple: true

      - name: Aggregate exact release security evidence
        run: |
          python3 scripts/verify_release_security_evidence.py aggregate \
            --evidence-dir "$RUNNER_TEMP/release-security" \
            --digests-dir "$RUNNER_TEMP/release-digests" \
            --exceptions security/release-image-exceptions.json \
            --trivy-ignore security/trivy-release-ignore \
            --repository "$GITHUB_REPOSITORY" \
            --source-commit "$GITHUB_SHA" \
            --build-run-id "$GITHUB_RUN_ID" \
            --build-run-attempt "$GITHUB_RUN_ATTEMPT" \
            --output "$RUNNER_TEMP/release-security-evidence.json"
          (cd "$RUNNER_TEMP" && sha256sum release-security-evidence.json \
            > release-security-evidence.json.sha256)

      - name: Render immutable production release
        env:
          DIGEST_DIR: ${{ runner.temp }}/release-digests
          IMAGE_BASE: ghcr.io/${{ github.repository }}
          OUTPUT: ${{ runner.temp }}/production-release.yaml
          RELEASE_SHA: ${{ github.sha }}
        run: |
          IMAGE_BASE=$(printf '%s' "$IMAGE_BASE" | tr '[:upper:]' '[:lower:]')
          export IMAGE_BASE
          ./scripts/render-release-manifest.sh
          output_dir=$(dirname -- "$OUTPUT")
          output_name=$(basename -- "$OUTPUT")
          (cd "$output_dir" && sha256sum "$output_name" > "${output_name}.sha256")

      - name: Render immutable staging release from the same digests
        env:
          APP_ENV: staging
          AUTH_HOST: auth.staging.mnema.app
          DIGEST_DIR: ${{ runner.temp }}/release-digests
          IMAGE_BASE: ghcr.io/${{ github.repository }}
          INCLUDE_INGRESS: "true"
          OUTPUT: ${{ runner.temp }}/staging-release.yaml
          PUBLIC_HOST: staging.mnema.app
          RELEASE_NAMESPACE: mnema-staging
          RELEASE_SHA: ${{ github.sha }}
          S3_ENDPOINT: http://minio:9000
          S3_PATH_STYLE_ACCESS: "true"
          S3_PUBLIC_ENDPOINT: https://storage.staging.mnema.app
          SPRING_PROFILES: prod
        run: |
          IMAGE_BASE=$(printf '%s' "$IMAGE_BASE" | tr '[:upper:]' '[:lower:]')
          export IMAGE_BASE
          ./scripts/render-release-manifest.sh
          output_dir=$(dirname -- "$OUTPUT")
          output_name=$(basename -- "$OUTPUT")
          (cd "$output_dir" && sha256sum "$output_name" > "${output_name}.sha256")

      - name: Bind verified security evidence to both release manifests
        run: |
          for manifest in production-release.yaml staging-release.yaml; do
            python3 scripts/verify_release_security_evidence.py verify-release \
              --evidence "$RUNNER_TEMP/release-security-evidence.json" \
              --manifest "$RUNNER_TEMP/$manifest" \
              --expected-repository "$GITHUB_REPOSITORY" \
              --expected-commit "$GITHUB_SHA" \
              --trivy-ignore security/trivy-release-ignore \
              --expected-run-id "$GITHUB_RUN_ID"
          done

      - name: Upload complete release manifest
        uses: ./.github/actions/safe-upload-artifact
        with:
          policy-id: main-production-release-manifest
          name: production-release-manifest
          path: |
            ${{ runner.temp }}/production-release.yaml
            ${{ runner.temp }}/production-release.yaml.sha256
            ${{ runner.temp }}/release-security-evidence.json
            ${{ runner.temp }}/release-security-evidence.json.sha256
          if-no-files-found: error
          retention-days: 30
          classification: release-manifest

      - name: Upload staging release manifest
        uses: ./.github/actions/safe-upload-artifact
        with:
          policy-id: main-staging-release-manifest
          name: staging-release-manifest
          path: |
            ${{ runner.temp }}/staging-release.yaml
            ${{ runner.temp }}/staging-release.yaml.sha256
            ${{ runner.temp }}/release-security-evidence.json
            ${{ runner.temp }}/release-security-evidence.json.sha256
          if-no-files-found: error
          retention-days: 30
          classification: release-manifest
