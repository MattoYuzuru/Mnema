# CodeQL tracing raises compiler memory pressure above Gradle's 512 MiB default.
# Give the Gradle and Kotlin daemons enough heap for hosted tracing and local builds.
org.gradle.jvmargs=-Xmx1500m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
kotlin.daemon.jvmargs=-Xmx1500m -Xms256m
